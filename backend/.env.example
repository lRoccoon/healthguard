# Example environment variables for HealthGuard Backend

# Security
SECRET_KEY=change-this-to-a-secure-random-string-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# App Settings
DEBUG=true
APP_NAME="HealthGuard AI"
APP_VERSION="1.0.0"

# Storage
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=./data

# LLM Provider Configuration
# Provider: "openai" (OpenAI-compatible) or "volcengine" (火山引擎)
LLM_PROVIDER=openai
LLM_API_KEY=your-llm-api-key
# LLM_MODEL=gpt-4o                          # Model name (uses provider default if not set)
# LLM_BASE_URL=https://api.openai.com/v1    # Custom base URL (uses provider default if not set)
# LLM_API_MODE=chat                          # "chat" for chat/completions, "responses" for responses API

# Volcano Engine (火山引擎) example:
# LLM_PROVIDER=volcengine
# LLM_API_KEY=your-volcengine-api-key
# LLM_MODEL=doubao-1-5-pro-256k-250115
# LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# Web Search (Tavily)
# WEB_SEARCH_API_KEY=your-tavily-api-key
# WEB_SEARCH_PROVIDER=tavily

# Feishu (Lark) Bot
# FEISHU_APP_ID=your-feishu-app-id
# FEISHU_APP_SECRET=your-feishu-app-secret
# FEISHU_VERIFICATION_TOKEN=your-verification-token
# FEISHU_ENCRYPT_KEY=your-encrypt-key

# CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080
